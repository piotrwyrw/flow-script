# -- Line comments start with the hash symbol --

# Function definitions
fn do_stuff(param1, param2, param3) {
    # TODO
}

# Functions are expressions + Functions can be anonymous
let callback = fn() {
    return 123;
}